#pragma once
/* UnX-style gameplay boosters + debug cheats — 30Hz timer writes.
 * Gate: dashboard items (boosters.*, cheats.*). Toggle ON = timer active. */
namespace FfxHooks { bool StartUnXBoosterHook(); }
